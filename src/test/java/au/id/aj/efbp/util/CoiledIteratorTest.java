package au.id.aj.efbp.util;

import static org.junit.Assert.assertFalse;

import java.util.Collections;
import java.util.Iterator;

import org.junit.Test;

public class CoiledIteratorTest {
    @Test
    public void resumeHasNextZeroElements() {
        /*
        final Iterator<Object> cycle =
            new CyclicIterator<>(Collections.emptyList());
        final Iterator<Object> coil = new CoiledIterator<Object>(cycle);
        assertFalse(coil.hasNext());
        */
    }

    @Test
    public void resumeNext() {
    }

    @Test
    public void resumeRemove() {
    }
}
